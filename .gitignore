# Node
node_modules/
dist/
*.tsbuildinfo
.vite/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage

# Env / secrets
.env
.env.*
!.env.example
# frontend/.env.production holds ONLY public build-time values (public PKCE client_id + URLs — the same
# values baked into the shipped JS bundle), so it is committed for a reproducible prod build.
!.env.production

# OS / editor
.DS_Store
Thumbs.db

# SQLite app DB
*.db
*.sqlite
*.sqlite3
